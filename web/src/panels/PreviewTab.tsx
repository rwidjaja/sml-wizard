/** Placeholder for the data preview tab - content/behavior to be filled in
 *  per the user's own code and instructions. */
export function PreviewTab() {
  return (
    <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--as-muted)' }}>
      Preview coming soon.
    </div>
  )
}
